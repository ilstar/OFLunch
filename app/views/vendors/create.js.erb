$("#vendors_table").append("<%= escape_javascript(render(partial: 'tr_vendor', locals: {vendor: @vendor})) %>");

$("#modal_form").modal('hide');

// reset form
<% @vendor = Vendor.new %>
$("#modal_form").html("<%= escape_javascript(render(partial: 'modal_form')) %>");

jQuery('.best_in_place').best_in_place();

