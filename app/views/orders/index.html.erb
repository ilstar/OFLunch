<% if @today_meal_time %>
  <% if @today_order.blank? %>
    <%= link_to t('.order'), new_order_path %>
  <% else %>
    <%= link_to t('.my_order'), edit_order_path(@today_order) %>
  <% end %>
<% else %>
  <%= t '.no_mom' %>
<% end %>
