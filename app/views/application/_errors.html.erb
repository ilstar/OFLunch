<% if target.errors.any? %>
  <div class="alert alert-error">
    <a class="close" data-dismiss="alert">×</a>
    <% target.errors.each_with_index do |(k, v), index| %>
      <%= "#{index + 1}. " %><%= v  %>
    <% end %>
  </div>
<% end %>
