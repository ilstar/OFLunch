<h1>Listing menu_items</h1>
<%= link_to 'New Menu item', new_vendor_menu_item_path(@vendor) %>

<br/>

<table>
  <tr>
    <th>Name</th>
    <th>Price</th>
    <th>Serial num</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @menu_items.each do |menu_item| %>
  <tr>
    <td><%= menu_item.name %></td>
    <td><%= menu_item.price %></td>
    <td><%= menu_item.serial_num %></td>
    <td><%= link_to 'Edit', edit_vendor_menu_item_path(@vendor, menu_item) %></td>
    <td><%= link_to 'Destroy', vendor_menu_item_path(@vendor, menu_item),  confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Menu item', new_vendor_menu_item_path(@vendor) %>
